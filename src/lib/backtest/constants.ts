/**
 * Risk defaults — aligned with live algo (python_algo/main.py).
 * Keep in sync so Strategy Validator matches live behavior.
 */
export const RISK_PER_TRADE_DEFAULT = 0.01; // 1%
export const MAX_DAILY_LOSS_USD = 50;
export const INITIAL_EQUITY_DEFAULT = 1000;
